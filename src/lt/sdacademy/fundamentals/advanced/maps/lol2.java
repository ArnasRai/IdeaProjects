package lt.sdacademy.fundamentals.advanced.maps;

import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;

public class lol2 {
   /* public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);



        private static Map<String, Integer> countries (Scanner) {
                Map<String, Integer> countries2 = new HashMap<>();
        while (true)
            for (Map.Entry<String, Integer> dictionary : countries.entrySet()) {
                System.out.println("name?");
                String name = scanner.next();
                System.out.println("age:?");
                int age = scanner.nextInt();
                if (name.equalsIgnoreCase("stop")) {
                    break;
                }
                if (String.valueOf(age).contains("[a-zA-Z]+")) {
                    break;
                }
                countries.put(name, age);
                System.out.println(countries);
            }



}
    }
}*/
}